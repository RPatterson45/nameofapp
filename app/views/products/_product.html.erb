<div class="col-md-3 col-xs-12" id="treats">
	<%= image_tag(product.image_url, class: "img-responsive") %>
  	<h1><%= product.name %></h1>
    <p><%= product.description %></p>
    <strong>$<%= product.price %> each</strong>
    <%= link_to "Edit product", edit_product_path(product) %>
</div>