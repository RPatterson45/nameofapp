<div class="col-md-4 col-xs-12" id="treats">
	<%= image_tag(product.image_url, class: "img-responsive") %>
  	<h1><%= product.name %></h1>
    <p><%= product.description %></p>
    <strong>$<%= product.price %> each</strong>
</div>